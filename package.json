{
  "name": "my-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "rm -rf _site && npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "build:prod": "ENV=production npx @11ty/eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-beta.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0"
  }
}
